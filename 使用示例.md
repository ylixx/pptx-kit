# 🚀 Intent 模式使用示例

## 📋 正确工作流程

### 🎯 核心理念
- **AI 负责内容**：AI 直接生成 `storyboard.json`
- **系统负责版式**：自动将 intent 映射为具体版式
- **工具辅助**：`quick_start.py` 只生成提示词，不生成内容

### 步骤 1：准备文件
```
pptx-kit/
├── PROMPT.md              # 已更新，支持 Intent 模式
├── quick_start.py         # AI 提示词生成器
├── 你的论文.txt           # 你的论文文件
└── ...
```

### 步骤 2：生成 AI 提示词
```bash
cd pptx-kit/
python3 quick_start.py
```

脚本会：
1. 自动读取你的论文文件
2. 生成完整的 AI 提示词
3. 显示提示词供你复制

### 步骤 3：AI 生成 storyboard.json
1. 复制 `quick_start.py` 生成的提示词给 AI
2. AI 直接生成 `storyboard.json`（包含完整 JSON）
3. 将 AI 返回的 JSON 粘贴给 `quick_start.py`
4. 脚本自动保存为 `storyboard.json`

### 步骤 4：渲染 PPTX
```bash
python3 render_pptx.py storyboard.json -o 答辩.pptx
```

## 🎯 Intent 模式优势

### 传统方式 vs Intent 方式

| 对比项 | 传统方式 | Intent 方式 |
|--------|----------|-------------|
| AI 记忆负担 | 49 个版式名 | 39 个 intent |
| 版式选择 | 固定版式 | 多候选自动选择 |
| 人工调整 | 需重写 JSON | 可视化切换 |
| 扩展性 | 需改代码 | 只改 JSON |

### Intent 分类速查

| Intent | 说明 | 自动映射版式 |
|--------|------|-------------|
| `cover` | 封面 | cover |
| `points-3` | 三列要点 | text-3, steps-3, mockup-3, imgtext-3 |
| `timeline-4` | 四节点时间轴 | timeline-4, timeline2-5 |
| `imgtext-3` | 三图三文 | imgtext-3 |
| `table` | 数据表格 | table |
| `chart-pie` | 饼图 | chart-pie |
| `swot` | SWOT分析 | swot, swot2 |

## 🔄 实际工作流程

### 流程图解
```
你的论文 → quick_start.py → AI 提示词 → AI 生成 storyboard.json → render_pptx.py → PPTX
```

### 关键区别
- **quick_start.py 不是生成器**：它只生成提示词，不生成内容
- **AI 是生成器**：AI 直接生成完整的 `storyboard.json`
- **系统是解析器**：自动将 intent 映射为具体版式

## 📝 实际使用示例

### 📝 实际使用示例

#### 示例 1：运行 quick_start.py
```bash
cd pptx-kit/
python3 quick_start.py
```

#### 示例 2：AI 提示词生成
脚本会显示类似这样的提示词：

```
请将以下论文重构成 storyboard.json，只输出 JSON，不要任何解释。

重要：使用 Intent 模式（pattern 字段写内容意图，不要写具体版式名）。
系统会自动将 intent 映射为最合适的具体版式。

论文内容：
---
[你的论文全文内容]
---

现在请输出完整的 storyboard JSON：
```

#### 示例 3：AI 直接生成 storyboard.json
你将提示词复制给 AI，AI 直接返回完整的 JSON：

```json
{
  "title": "基于深度学习的文本分类研究",
  "pages": [
    {
      "pattern": "cover",
      "fields": {
        "title": "基于深度学习的文本分类研究",
        "en": "Deep Learning for Text Classification",
        "advisor": "指导老师：张三 教授",
        "presenter": "答辩人：李四"
      },
      "notes": "各位老师好，我是李四，我的论文题目是……"
    },
    {
      "pattern": "points-3",
      "fields": {
        "title": "三个创新点",
        "s1": "创新点1",
        "b1": "详细描述1",
        "s2": "创新点2", 
        "b2": "详细描述2",
        "s3": "创新点3",
        "b3": "详细描述3"
      }
    }
  ]
}
```

#### 示例 4：粘贴 JSON 并保存
将 AI 返回的 JSON 粘贴给 `quick_start.py`，脚本会：
1. 验证 JSON 格式
2. 自动保存为 `storyboard.json`
3. 显示下一步操作

#### 示例 5：系统自动解析
运行 `render_pptx.py` 时，系统会：
- `points-3` → 自动选中最合适的版式（如 `text-3`）
- `cover` → 直接使用 `cover` 版式
- 生成最终 PPTX

## 🔧 高级功能

### 可视化版式调整
```bash
# 导出缩略图
python3 export_thumbs.py 答辩.pptx -o thumbs/

# 生成交互式切换器
python3 make_viewer.py storyboard.json -t thumbs/ -o viewer.html
```

双击 `viewer.html` 可以：
- 右键页面 → 弹出同类候选版式
- 点击候选 → 实时预览效果
- 导出修改后的 JSON

### 校验工具
```bash
# 检查 layout_index 是否正确
python3 validate_layout_index.py
```

## 📊 效率对比

| 操作 | 传统方式 | Intent 方式 | 提升 |
|------|----------|-------------|------|
| AI 生成时间 | 需记忆版式名 | 只需写 intent | ↓30% |
| 版式选择灵活性 | 固定 | 多候选自动选择 | ↑300% |
| 人工调整成本 | 高（重写 JSON） | 低（可视化切换） | ↓80% |
| 扩展新版式 | 需改代码 | 只改 JSON | ↑100% |

## 🎯 最佳实践

1. **给 AI 的提示**：强调使用 intent 模式
2. **批量生成**：同一 intent 可生成多个候选版式
3. **人工微调**：用 `viewer.html` 可视化调整
4. **版本管理**：保存原始 intent 版 JSON

现在你可以享受**智能意图驱动**的 PPT 生成体验了！