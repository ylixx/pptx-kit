# 使用手册 - 用网页版大模型制作答辩 PPTX

> 适用对象:**不会编程、不依赖任何 AI 助手**的普通用户。
> 你只需要三样东西:1 这个 `pptx-kit` 文件夹;2 一个网页版大模型(ChatGPT / DeepSeek /
> Kimi / 豆包 / 通义千问......任选其一);3 一台装了 Python 的电脑(装一次,10 分钟)。
>
> 全流程 7 步,首次约 40 分钟,之后每篇论文约 15 分钟。

```
论文 ──第2~4步(网页大模型)──▶ storyboard.json ──第5步(本地一条命令)──▶ 答辩PPT.pptx
                                        └────── 第6步:PPT里润色10分钟 ──────┘
```

---

## 第 1 步:准备材料(5 分钟)

在电脑上建一个工作文件夹,比如 `D:\答辩PPT\`,放入:

| 文件 | 从哪来 | 作用 |
|---|---|---|
| `pptx-kit` 整个文件夹 | 你已有的这套工具 | 提示词 + 渲染器 + 模板 |
| `论文.md`(或 .txt) | 你的论文/答辩文稿 | 内容来源 |

**论文整理要求**(很重要,直接影响出片质量):
- 把 Word 论文全选复制,粘贴进记事本/VS Code,存成 `论文.md` 或 `论文.txt`
- 保留标题层级(第1章、1.1 这种),删掉目录、页眉页脚、致谢
- 如果有图表,用一句话在正文标注:`【图3】系统架构图:左侧是数据层......`(第 6 步会用到)
- 字数不限,3000~30000 字都行

---

## 第 2 步:打开网页版大模型(1 分钟)

1. 浏览器打开你常用的对话式大模型(推荐带**文件上传**功能的,如 ChatGPT / DeepSeek / Kimi / 豆包)
2. **新建对话**(不要在旧对话里继续,避免上下文污染)
3. 登录你的账号即可,无需开通付费功能

> 选型提示:哪家模型都行,但要选"会写 JSON 的"(主流模型都没问题)。
> 如果输出老出错,换一家试试,不同模型风格差异挺大。

---

## 第 3 步:投喂提示词 + 论文(3 分钟)

### 方式 A：模型支持上传文件（推荐）

1. 点击输入框旁的 **回形针 📎 / 上传文件** 按钮
2. **只上传两个文件**，其他都不要传：

| 顺序 | 文件 | 完整位置 | 说明 |
|---|---|---|---|
| ① | `PROMPT.md` | `D:\答辩PPT\pptx-kit\PROMPT.md` | 原样上传，不用改（给模型的“工作手册”） |
| ② | 论文文件 | `D:\答辩PPT\论文.md`（或论文的 Word/PDF 原件） | 必要时直接传 .docx/.pdf 即可，模型读得懂 |

❌ **不要传**：`render_pptx.py`、`patterns.json`、`storyboard.schema.json`、
`assets/template.pptx`、手册和 README——这些是本地渲染用的，模型用不上，反而干扰。

3. 在输入框粘贴下面这段话，点发送：

```
请仔细阅读我上传的 PROMPT.md 和论文文件,严格按 PROMPT.md 的要求,
把论文重构成 storyboard JSON。只输出 JSON 本身,不要输出任何解释文字,
不要用 markdown 代码块包裹。答辩人写"李明",指导老师写"王华 教授"。
```

### 方式 B:模型不支持上传(纯粘贴)

1. 用记事本打开 `pptx-kit/PROMPT.md`,**全选复制**,粘贴进输入框
2. 空一行,输入 `====== 以下是论文 ======`,再把 `论文.md` 全文粘上
3. 最后粘贴方式 A 第 3 点的那段话,点发送

> 提示词很长(约 5500 字)+ 论文很长,超出了也没关系,主流模型都吃得下。
> 如果模型说"内容太长",把论文拆成两半分两次发,第二次说"请继续输出剩余 pages"。

### 此时模型会输出什么

一大段 JSON,长这样(节选):

```json
{
  "title": "基于XXX的YYY研究",
  "pages": [
    { "pattern": "cover", "fields": { "title": "基于XXX的YYY研究", ... }, "notes": "各位老师好......" },
    { "pattern": "section", "fields": { "num": "01", "title": "研究背景", ... } },
    ...(共 15~25 页)
  ]
}
```

**你要看的只有两件事**:
1. 页数在 15~25 之间吗?(`"pattern"` 出现的次数就是页数)
2. 每页有没有 `"notes"`(答辩讲稿)?

---

## 第 4 步:拿到 storyboard.json(3 分钟)

1. 点击模型回复右上角的 **复制 📋** 按钮(或全选文本复制)
2. 打开记事本(Windows)/ 文本编辑(Mac),粘贴
3. **另存为** `storyboard.json`,放进工作文件夹 `D:\答辩PPT\`
   - ⚠️ 保存类型选"所有文件",编码选 **UTF-8**(记事本默认就是)
   - ⚠️ 文件名必须以 `.json` 结尾,不能是 `storyboard.json.txt`

> 复制时带没带 ```json 代码块标记都没关系--渲染器会自动剥掉。

### 自检清单(30 秒)

| 检查项 | 不合格怎么办 |
|---|---|
| 开头是 `{`,结尾是 `}` | 说明复制不全,重新复制整段 |
| `pattern` 出现 15~25 次 | 太多/太少就回模型说:"请压缩到 18 页左右" |
| 没有 `null`、`???`、`待补充` 之类 | 回模型说:"请补全所有字段,不要占位符" |
| 模型没在 JSON 外面加解释文字 | 没关系,渲染器能自动提取 |

**发现问题直接回模型修**,就当聊天:

```
请修正:第 5 页的 body 字段超长了,请压缩到 36 字以内;
第 12 页请换成 chart-line 版式,用论文表 3 的准确率数据。
重新输出完整 JSON。
```

每次它都会给你一份完整的新 JSON,重复第 4 步保存即可。

---

## 第 5 步:本地渲染出片(10 分钟,仅首次要装环境)

### 5.1 首次:安装 Python(已装可跳过)

**Windows:**
1. 打开 <https://www.python.org/downloads/>,下载最新版 Python,双击安装
2. ⚠️ 安装第一屏务必勾选 **"Add python.exe to PATH"**,再点 Install Now
3. 按 `Win + R`,输入 `cmd` 回车,打开黑色命令行窗口

**Mac:**
1. 打开"终端"(启动台搜 Terminal)
2. 输入 `python3 --version` 回车,有版本号就不用装

### 5.2 首次:安装依赖(一条命令)

在命令行窗口输入:

```
pip install python-pptx
```

Mac 如果报错,换成 `pip3 install python-pptx`。看到 `Successfully installed` 就成功了。

### 5.3 每次:渲染出片(一条命令)

1. 命令行切换到工作文件夹:

```
D:
cd \答辩PPT
```

(Mac:`cd ~/答辩PPT`)

2. 运行渲染:

```
python pptx-kit\render_pptx.py storyboard.json -o 答辩PPT.pptx
```

(Mac 把 `\` 换成 `/`:`python3 pptx-kit/render_pptx.py storyboard.json -o 答辩PPT.pptx`)

3. 看到下面这行就是成功:

```
✅ 已生成 答辩PPT.pptx(21 页,2 条警告)
```

4. (可选)自动质检,检查页数/占位残留/字段对位/图表数据:

```
python pptx-kit\verify_pptx.py storyboard.json 答辩PPT.pptx
```

看到 `✅ 质检通过(0 项失败 / N 项提醒)` 就放心了;`❌ 质检未通过` 则按提示回模型修正。

### 5.4 看懂输出

| 输出 | 含义 | 处理 |
|---|---|---|
| `✅ 已生成 ...` | 出片成功 | 打开 PPT 看看 |
| `[warn] 槽位 xx 超长` | 某处文字可能挤出文本框 | 记下页码,第 6 步删几个字 |
| `[warn] 图片不存在` | images 里写的图没找到 | 忽略,第 6 步手动补图 |
| `[warn] 未知槽位` | 模型写错了字段名 | 一般无害;多了就回模型要求修正 |
| `[warn] pattern 未知 ... 已兜底替换` | 模型编了不存在的版式,渲染器已自动换成字段兼容的版式 | 一般无害,打开 PPT 看看排版是否满意 |
| `[warn] 图表系列 ... 不一致,已截断` | 图表的数值个数与分类个数对不上 | 回模型修正数据,或第 6 步右键图表改数据 |

---

## 第 6 步:PPT 里润色(10 分钟)

双击打开 `答辩PPT.pptx`(PowerPoint / WPS 都行),按顺序过一遍:

1. **补图片**(最大的工作量)
   - 图文页的空占位符:单击占位符中间的图标 🖼,选你的图片即可(模板已配好裁切框)
   - 框架图、实验截图建议直接从论文 PDF 截图粘贴
2. **图表数据**:右键图表 → **编辑数据**,在弹出的 Excel 里改成你的真实数据
3. **表格**:如果右侧多出空列(模板是 9 列),右键 → **删除列**
4. **超长文字**:按第 5 步警告的页码,把溢出的字删掉(内容宁少勿多)
5. **封面信息**:检查答辩人、指导老师、学校名
6. **过一遍备注区**(每页的讲稿):顺手改成自己的口语习惯

---

## 第 7 步:迭代改稿

| 改动规模 | 怎么改 |
|---|---|
| 改几个字、换一页版式 | 直接改 `storyboard.json`(例:把 `"pattern": "text-3"` 改成 `"text-6"`),重新渲染,1 分钟 |
| 大改(加一章、砍一章、换风格) | 回到网页模型的**同一个对话**里说要求,拿新 JSON 重新保存+渲染 |
| 全盘重排 | 新开对话,从第 3 步重来(在发送语里加上你的新偏好) |

**改 JSON 的小抄**--每页就是一块这样的东西,字段名对照 `pptx-kit/PROMPT.md` 里的版式目录表:

```json
{
  "pattern": "text-3",                 ← 改这里换版式
  "fields": { "title": "...", "s1": "...", "b1": "..." },   ← 改这里换文字
  "notes": "讲稿......"
}
```

改完存盘,重跑第 5.3 步的命令即可。**想把偏好固化下来**(比如"全篇少用图表页"、
"多用图片"),就把要求写进发送语,或者让模型写进一个 `偏好.txt` 以后每次带上。

---

## 常见问题(FAQ)

**Q1:模型输出的 JSON 一保存就说格式错误?**
先确认存的是 UTF-8、文件名不是 `.txt`。还是不行就在对话里说:
"你输出的 JSON 格式有误,用 JSON 校验器检查后重新输出完整版本。"
渲染器会自动剥掉代码块标记和前后缀文字,所以通常不是这个问题。

**Q2:想用模板里指定的某一页版式怎么办?**
在发送语里直接点名,例如:"方法部分那页请用 `process-5`,实验对比用 `chart-line`。"
不知道有哪些版式可选:命令行跑 `python pptx-kit\render_pptx.py --list-patterns`,
49 种版式连字段名一起列出来,挑好 id 抄进发送语即可。

**Q3:一页要放两张图表?**
用 `charts` 写法(详见 PROMPT.md 末尾示例),`chart-donut3`(三圆环)和
`compare-data`(双圆环)就是这种多图版式。懒人方案:让模型填,出片后右键改数据。

**Q4:渲染出来的字体、配色能改吗?**
模板用微软雅黑/Arial,主题色可在 PowerPoint 的 **设计 → 变体 → 颜色** 一键换
(换成你校徽的颜色最出彩)。字体在 **开始 → 替换字体** 一键全换。

**Q5:能把 PPTX 转成 PDF 交材料吗?**
PowerPoint/WPS:文件 → 另存为 → PDF。PPTX 是原生可编辑格式,答辩前改字很方便。

**Q6:换了一套新 PPT 模板,这套工具还能用吗?**
能。让模型(或任何会写代码的 AI)照着 `patterns.json` 的格式重新数一遍新模板的
形状索引即可,渲染器不用动。具体见 README 的 FAQ。

**Q7:我的论文涉密/隐私,能用网页模型吗?**
不能。涉密内容请用本地部署的开源模型(如 Ollama + Qwen),
操作完全一样--把第 2 步换成打开本地对话界面即可。

---

## 附录:一次完整对话长什么样(压缩实录)

```
你:(上传 PROMPT.md + 论文.md)
    请仔细阅读我上传的 PROMPT.md 和论文文件,严格按 PROMPT.md 的要求,
    把论文重构成 storyboard JSON。只输出 JSON 本身......答辩人写"李明"。

AI:{ "title": "......", "pages": [ ...21 页... ] }

你:第 8 页 chart-line 的数据换用论文表 4 的召回率;参考文献压缩到 5 条。
    重新输出完整 JSON。

AI:{ ...修正后的完整 JSON... }

你:很好,把第 13 页的 compare-2 换成 two-views,讲稿语气更口语一点。

AI:{ ...最终版 JSON... }

(复制保存为 storyboard.json → 命令行一条命令渲染 → PPT里补图润色 → 完成)
```

整个过程里,模型只负责"写什么",渲染器负责"长什么样"--
这就是为什么换任何一家网页模型,出来的排版都一样漂亮、稳定、可复现。
